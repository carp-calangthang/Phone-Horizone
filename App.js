import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

import { AppNavigator } from './Apps/Utils/AppNavigator';

export default function App() {
  return (
    <AppNavigator />
  );
}
